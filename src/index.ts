import {IMetric, Metric} from './metric'; 
import {IPoint, Point} from './point'; 
import {LinePlot} from './graphs/linePlot'; 

module.exports = {
    Metric, Point, LinePlot
  };

export { IMetric, Metric, IPoint, Point, LinePlot };